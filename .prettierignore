package-lock.json
pnpm-lock.yaml
yarn.lock

# Generated output — never hand-formatted, so it must not fail `pnpm check`.
# Regenerated by @tanstack/router-plugin on every dev/build run.
src/routeTree.gen.ts
# Regenerated by `pnpm dlx shadcn@latest add <component>`; reformatting them
# would be undone the next time a primitive is added or updated.
src/components/ui/
dist/
.tanstack/

# Requirement 7.3: each fixture must stay byte-identical to the corresponding
# fenced example in docs/upstream-api.md. Prettier must never touch these.
fixtures/upstream/
