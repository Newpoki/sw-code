node_modules
.DS_Store
dist
dist-ssr
*.local

# environment: secrets stay local, the documented template stays tracked
.env
.env*
!.env.example

# runtime JSON store (DATA_FILE), created on first write
data/

.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# typescript
*.tsbuildinfo
